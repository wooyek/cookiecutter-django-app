# These are tools that don't necessarily have to be installed alongside the app
# in it's virtual environment but are needed as executables on path.
# They are here to ease environment preparation eg. in travis.yml.
bump2version
check-manifest
codecov
coverage
flake8
invoke
isort
pip
pygments
readme-renderer
setuptools
tox
wheel
