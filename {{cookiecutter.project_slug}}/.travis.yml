language: python

python:
  - "3.5"

env:
  - TOX_ENV=py34-django-111
  - TOX_ENV=py35-django-111
  - TOX_ENV=py36-django-111
  - TOX_ENV=py34-django-20
  - TOX_ENV=py35-django-20
  - TOX_ENV=py36-django-20
  - TOX_ENV=py34-django-master
  - TOX_ENV=py35-django-master
  - TOX_ENV=py36-django-master

matrix:
  fast_finish: true

before_install:
  - python --version
  - uname -a
  - lsb_release -a
  - sudo apt-get install -y git-flow python-enchant

install:
  - pip install -r requirements/development.txt coveralls
  - virtualenv --version
  - easy_install --version
  - pip --version
  - tox --version
  - git --version

script:
  - tox -e $TOX_ENV

jobs:
  include:
    - stage: make
      script:
          - which python
          - tox --version
          - pwd
          - ls -la
          - make
          - make bump
          - make lint
          - make test
          - make test-all
          - make coverage
          - make dist
          - make install
          - make docs
          # Lint again after docs generation
          - make lint
          - make clean

after_success:
  - bash <(curl -s https://codecov.io/bash)
  - codecov -e TOX_ENV
  - coveralls

notifications:
  email:
    on_success: never
    on_failure: always
